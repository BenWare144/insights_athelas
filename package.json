{
  "name": "athelas-compact",
  "version": "15.10.0",
  "private": true,
  "description": "Compact mode + chart-note helpers for Athelas Insights (userscript + Chrome extension)",
  "type": "commonjs",
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  }
}
